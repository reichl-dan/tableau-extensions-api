/**
 * Represents the type of encoding used for visualization images.
 */
export enum VizImageEncodingType {
  /**
   * Continuous encoding type (green "pill")
   */
  Continuous = 'continuous',
  /**
   * Discrete encoding type (blue "pill")
   */
  Discrete = 'discrete',
}
