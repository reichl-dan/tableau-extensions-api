/**
 * Represents the direction of sorting in Tableau.
 */
export enum SortDirection {
  /**
   * Sort in ascending order
   */
  Ascending = 'ascending',
  /**
   * Sort in descending order
   */
  Descending = 'descending',
}
