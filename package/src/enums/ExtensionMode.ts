/**
 * Represents the mode in which the extension is running.
 */
export enum ExtensionMode {
  /**
   * The extension is running in authoring mode
   */
  Authoring = 'authoring',
  /**
   * The extension is running in viewing mode
   */
  Viewing = 'viewing',
}
