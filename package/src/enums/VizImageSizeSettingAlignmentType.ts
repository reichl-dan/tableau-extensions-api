/**
 * Represents the alignment options for size settings in visualizations.
 */
export enum VizImageSizeSettingAlignmentType {
  /**
   * Right alignment
   */
  Right = 'right',
  /**
   * Left alignment
   */
  Left = 'left',
  /**
   * Center alignment
   */
  Center = 'center',
}
