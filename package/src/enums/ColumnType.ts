/**
 * Represents the type of column in Tableau.
 */
export enum ColumnType {
  /**
   * Continuous column type
   */
  Continuous = 'continuous',
  /**
   * Discrete column type
   */
  Discrete = 'discrete',
  /**
   * Unknown column type
   */
  Unknown = 'unknown',
}
