/**
 * Represents the different types of sheets in a Tableau workbook.
 */
export enum SheetType {
  /**
   * A dashboard sheet type
   */
  Dashboard = 'dashboard',
  /**
   * A story sheet type
   */
  Story = 'story',
  /**
   * A worksheet sheet type
   */
  Worksheet = 'worksheet',
}
