/**
 * Enum that represents the types of dialog popup styles.
 */
export enum DialogStyle {
  /**
   * Modal dialog style
   */
  Modal = 'modal',
  /**
   * Modeless dialog style
   */
  Modeless = 'modeless',
  /**
   * Window dialog style
   */
  Window = 'window',
}
