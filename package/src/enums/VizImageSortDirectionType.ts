/**
 * Represents the direction for sorting in visualizations.
 */
export enum VizImageSortDirectionType {
  /**
   * Sort in ascending order
   */
  Ascending = 'ascending',
  /**
   * Sort in descending order
   */
  Descending = 'descending',
}
