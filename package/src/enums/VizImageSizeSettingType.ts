/**
 * Represents the type of size setting for visualization elements.
 */
export enum VizImageSizeSettingType {
  /**
   * Fixed size setting with alignment and width in axis units
   */
  Fixed = 'fixed',
  /**
   * Manual size setting with mark size value
   */
  Manual = 'manual',
}
