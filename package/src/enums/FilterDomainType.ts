/**
 * The domain type for a filter.
 */
export enum FilterDomainType {
  /**
   * The filter domain is relevant
   */
  Relevant = 'relevant',
  /**
   * The filter domain is database
   */
  Database = 'database',
}
