/**
 * Represents the type of annotation in Tableau.
 */
export enum AnnotationType {
  /**
   * Area annotation type
   */
  Area = 'area',
  /**
   * Mark annotation type
   */
  Mark = 'mark',
  /**
   * Point annotation type
   */
  Point = 'point',
}
