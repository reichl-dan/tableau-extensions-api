/**
 * Represents the type of color palette used in visualizations.
 */
export enum VizImagePaletteType {
  /**
   * Custom diverging color palette with start and end colors
   */
  CustomDiverging = 'custom-diverging',
  /**
   * Custom sequential color palette with end color
   */
  CustomSequential = 'custom-sequential',
}
