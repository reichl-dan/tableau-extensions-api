/**
 * @since 1.4.0 Represents a logical table in a data source or a logical table used in a worksheet
 */
export interface LogicalTable {
  /**
   * The id of the logical table
   */
  id: string

  /**
   * The caption of the logical table
   */
  caption: string
}
